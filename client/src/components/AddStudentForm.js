import '../App.css';
import React from 'react';
import ChildProfileInfo from './ChildProfileInfo';
import { Link } from 'react-router-dom';

function AddStudentForm({ students }) {
    
    return (
        <div>
            <h1>Add a Student</h1>
            
        </div>
    );
}

export default AddStudentForm;
